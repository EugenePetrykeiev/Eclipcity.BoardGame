# Production infrastructure

Production is intentionally not implemented yet. It must use a separate backend
key, variables, IAM deployment role, capacity and availability decisions. Do not
point this directory at the dev state or copy dev cost-saving choices (single NAT
and one instance per role) without review.
